{
  "name": "pmis-console",
  "version": "1.0.0",
  "description": "pmis-console",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server build",
    "postinstall": "bower-installer",
    "build": "grunt copy",
    "publish": "grunt clean && grunt copy && grunt publish"
  },
  "author": "Emanuele Disco",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "typings": "^1.4.0",
    "bower": "latest",
    "bower-installer": "latest",
    "http-server": "latest",
    "grunt": "~0.4.5",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-compress": "^1.4.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-uglify": "~0.5.0"
  }
}
